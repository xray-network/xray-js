# XRAY JS documentation

```sh
npm install --workspaces=false
npm run dev --workspaces=false
```

Build with `npm run build --workspaces=false` and deploy the `wiki-xray-js-docs` Cloudflare Worker with `npm run deploy --workspaces=false`.
